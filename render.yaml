services:
  - type: web
    name: vivel-ai
    env: python
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
